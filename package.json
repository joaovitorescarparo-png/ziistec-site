{
  "name": "ziistec-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site institucional oficial da ZiisTec",
  "scripts": {
    "check": "node scripts/check.mjs",
    "prebuild": "npm run check && node --check site/script.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": "24.x"
  }
}
